# 🔧 Исправление подключения к базе данных

## Проблема:

Vercel (serverless) **не может подключиться** к Supabase напрямую:
```
Can't reach database server at db.qvmwbfvyihuuffacgmmb.supabase.co:5432
```

## Причина:

Для **serverless окружений** (Vercel, AWS Lambda) Supabase требует использовать **Connection Pooler** вместо прямого подключения!

## ✅ Решение:

### Шаг 1: Получи Pooler Connection String

1. Открой Supabase Dashboard (я уже открыл)
2. Перейди в **Settings** → **Database**
3. Прокрути вниз до раздела **Connection Pooling**
4. Найди **Connection string** в режиме **Transaction**
5. Нажми **Copy** рядом с URI

**Должно выглядеть примерно так:**
```
postgresql://postgres.qvmwbfvyihuuffacgmmb:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```

⚠️ **Важно:** Используй режим **Transaction**, НЕ Session!

### Шаг 2: Замени DATABASE_URL в Vercel

1. Открой Vercel → Settings → Environment Variables
2. Найди `DATABASE_URL`
3. Нажми **Edit**
4. **Замени** старое значение на новое (Pooler URL)
5. Нажми **Save**

### Шаг 3: Переразверни проект

1. Deployments → последний деплоймент
2. ••• → **Redeploy**
3. Подожди 2-3 минуты

### Шаг 4: Проверь

1. Открой `/login`
2. Нажми "Войти через Google"
3. **ДОЛЖНО ЗАРАБОТАТЬ!** ✅

---

## 💡 Почему это важно?

**Прямое подключение (порт 5432):**
- ❌ Не работает с serverless (Vercel)
- ❌ Медленное
- ❌ Ограничено количество подключений

**Connection Pooler (порт 6543):**
- ✅ Работает с serverless
- ✅ Быстрое
- ✅ Неограниченное количество подключений

---

## 🆘 Если не помогло:

Попробуй **Direct Connection** (только для теста):

1. В Supabase Dashboard → Database Settings
2. Найди **Connection string** (Direct connection)
3. Добавь в конец: `?connection_limit=1&pool_timeout=0`
4. Замени в Vercel и переразверни

Но лучше использовать Pooler!

