# 🔐 Настройка OAuth через ВКонтакте

Это пошаговая инструкция для настройки входа через ВКонтакте в вашем приложении.

---

## 📝 Шаг 1: Создание приложения ВКонтакте

1. **Перейдите на сайт разработчиков ВКонтакте:**
   - Откройте: https://dev.vk.com/
   - Войдите в свой аккаунт ВКонтакте

2. **Создайте новое приложение:**
   - Нажмите "Мои приложения" (или "My Apps")
   - Нажмите "Создать приложение"
   - Выберите тип: **"Веб-сайт"** или **"Standalone приложение"**

3. **Заполните форму:**
   - **Название:** ArtiGen (или название вашего проекта)
   - **Платформа:** Веб-сайт
   - **Адрес сайта:** 
     - Для разработки: `http://localhost:3000`
     - Для продакшена: ваш реальный домен (например, `https://yourdomain.com`)
   - **Базовый домен:** 
     - Для разработки: `localhost`
     - Для продакшена: ваш домен без https:// (например, `yourdomain.com`)

4. **Подтвердите создание**

---

## 🔑 Шаг 2: Получение Client ID и Secure Key

После создания приложения:

1. **Откройте настройки приложения:**
   - В списке "Мои приложения" найдите ваше приложение
   - Нажмите на него
   - Перейдите в раздел "Настройки"

2. **Скопируйте учетные данные:**
   
   Вы увидите два важных значения:
   
   - **ID приложения (App ID)** — это ваш `VK_CLIENT_ID`
     ```
     Например: 51234567
     ```
   
   - **Защищённый ключ (Secure key)** — это ваш `VK_CLIENT_SECRET`
     ```
     Например: ABCdef123XYZ456ghiJKL789
     ```

---

## ⚙️ Шаг 3: Настройка Redirect URI

Это очень важный шаг! ВКонтакте должен знать, куда перенаправлять пользователя после авторизации.

1. **В настройках приложения найдите раздел "Настройки" → "Доверенный redirect URI"**

2. **Добавьте следующие URL:**

   ### Для локальной разработки:
   ```
   http://localhost:3000/api/auth/callback/vk
   ```

   ### Для продакшена:
   ```
   https://yourdomain.com/api/auth/callback/vk
   ```

3. **Сохраните изменения**

> ⚠️ **Важно:** URL должен точно совпадать! Даже лишний `/` в конце может привести к ошибке.

---

## 🔐 Шаг 4: Настройка доступа к данным

1. **Перейдите в "Настройки" → "Права доступа"**

2. **Включите следующие разрешения:**
   - ✅ Доступ к email
   - ✅ Основная информация профиля

---

## 📦 Шаг 5: Добавление ключей в ваш проект

1. **Откройте файл `.env.local` в корне вашего проекта**

2. **Добавьте следующие строки:**

```env
# VK OAuth Configuration
VK_CLIENT_ID="ваш_app_id_здесь"
VK_CLIENT_SECRET="ваш_secure_key_здесь"
```

3. **Замените значения:**
   - Вместо `ваш_app_id_здесь` вставьте **ID приложения** (только цифры)
   - Вместо `ваш_secure_key_здесь` вставьте **Защищённый ключ**

### Пример правильного `.env.local`:

```env
# Существующие настройки NextAuth
NEXTAUTH_URL="http://localhost:3000"
AUTH_SECRET="ваш_секретный_ключ"
DATABASE_URL="file:./dev.db"

# Google OAuth (если есть)
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."

# VK OAuth — НОВЫЕ СТРОКИ
VK_CLIENT_ID="51234567"
VK_CLIENT_SECRET="ABCdef123XYZ456ghiJKL789"
```

4. **Сохраните файл**

---

## ✅ Шаг 6: Перезапуск сервера разработки

После добавления переменных окружения **обязательно перезапустите** сервер:

1. **Остановите сервер:**
   - В терминале нажмите `Ctrl+C`

2. **Запустите снова:**
   ```bash
   npm run dev
   ```

3. **Проверьте консоль:**
   
   Вы должны увидеть сообщение:
   ```
   ✅ VK OAuth provider configured
   📝 VK OAuth callback URL: http://localhost:3000/api/auth/callback/vk
   ```

---

## 🧪 Шаг 7: Тестирование

1. **Откройте страницу регистрации:**
   ```
   http://localhost:3000/register
   ```

2. **Вы должны увидеть кнопку "Войти через ВКонтакте"**

3. **Нажмите на неё:**
   - Вас перенаправит на страницу ВКонтакте
   - Разрешите доступ к данным
   - Вас вернёт обратно на ваш сайт уже авторизованным

---

## ⚠️ Частые проблемы и решения

### Проблема 1: "Invalid redirect_uri"

**Причина:** URL в настройках приложения не совпадает с тем, что использует NextAuth.

**Решение:**
1. Проверьте, что в настройках VK добавлен **точный** URL: `http://localhost:3000/api/auth/callback/vk`
2. Убедитесь, что нет лишних пробелов или символов
3. Проверьте, что `NEXTAUTH_URL` в `.env.local` — это `http://localhost:3000` (без `/` в конце)

---

### Проблема 2: "Access denied" или "Invalid client_id"

**Причина:** Неправильный Client ID или Client Secret.

**Решение:**
1. Скопируйте ID и Secret **заново** из настроек приложения VK
2. Убедитесь, что в `.env.local` нет лишних пробелов или кавычек внутри значений
3. Перезапустите сервер

---

### Проблема 3: Кнопка VK не появляется

**Причина:** Переменные окружения не загрузились или сервер не перезапущен.

**Решение:**
1. Убедитесь, что `VK_CLIENT_ID` и `VK_CLIENT_SECRET` есть в `.env.local`
2. Перезапустите сервер (остановите и запустите `npm run dev` снова)
3. Проверьте консоль — должно быть сообщение `✅ VK OAuth provider configured`

---

### Проблема 4: "Email not provided"

**Причина:** ВКонтакте не всегда предоставляет email пользователя.

**Решение:**
1. В настройках приложения VK включите "Доступ к email"
2. При первом входе VK попросит пользователя подтвердить доступ к email
3. Если пользователь откажет — вход не будет работать (это ограничение VK)

---

## 🎯 Готово!

Теперь у вас настроен вход через ВКонтакте! Пользователи могут:
- Регистрироваться через VK
- Входить через VK
- Связывать существующий аккаунт с VK (если email совпадает)

---

## 📚 Дополнительные ресурсы

- **Документация VK API:** https://dev.vk.com/ru/api/overview
- **OAuth VK:** https://dev.vk.com/ru/api/access-token/authcode-flow-user
- **NextAuth.js документация:** https://authjs.dev/

---

## 💡 Что дальше?

После настройки VK OAuth вы можете:
- Добавить другие провайдеры (Яндекс, Mail.ru)
- Настроить дополнительные разрешения VK (друзья, стена и т.д.)
- Кастомизировать внешний вид кнопок входа

