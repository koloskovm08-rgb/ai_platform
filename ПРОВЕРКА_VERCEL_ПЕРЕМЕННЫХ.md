# 🔍 Проверка переменных Vercel - Детальная инструкция

## Проблема

Код в `route.ts` проверяет переменные так:
```typescript
if (!googleClientId || !googleClientSecret || 
    googleClientId.trim() === '' || googleClientSecret.trim() === '') {
  // Возвращает ошибку "Configuration"
}
```

Это значит, что даже если переменная существует, но содержит **только пробелы** или **пустая** — будет ошибка!

## ✅ Что нужно проверить в Vercel

### 1. GOOGLE_CLIENT_ID

**Правильное значение (из .env.local):**
```
1024578853768-el77m1mddsgpeqb5sg7d6hinh22l69r0.apps.googleusercontent.com
```

**Проверь:**
- ✅ Нет пробелов в начале
- ✅ Нет пробелов в конце
- ✅ Нет переносов строк
- ✅ Точно совпадает с Google Console

### 2. GOOGLE_CLIENT_SECRET

**Правильное значение (из .env.local):**
```
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
```

**Проверь:**
- ✅ Нет пробелов в начале
- ✅ Нет пробелов в конце
- ✅ Нет переносов строк
- ✅ Точно совпадает с Google Console

### 3. NEXTAUTH_URL

**Правильное значение:**
```
https://koloskovm08-rgb-aiplatform.vercel.app
```

**Проверь:**
- ✅ Нет пробелов в начале
- ✅ Нет пробелов в конце
- ✅ Нет переносов строк
- ✅ Нет `/` в конце

## 📋 Пошаговая инструкция

### Шаг 1: Открой Environment Variables в Vercel
1. Vercel Dashboard → Твой проект
2. Settings → Environment Variables

### Шаг 2: Для КАЖДОЙ переменной (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, NEXTAUTH_URL)

1. Нажми **Edit** (карандаш)
2. **Скопируй значение** в блокнот
3. **Проверь в блокноте:**
   - Есть ли пробелы в начале? → Удали
   - Есть ли пробелы в конце? → Удали
   - Есть ли пустые строки? → Удали
4. **Скопируй чистое значение** обратно
5. Вставь в Vercel
6. Нажми **Save**

### Шаг 3: Убедись, что Environments выбраны правильно

Для каждой переменной должны быть выбраны:
- ✅ Production
- ✅ Preview
- ✅ Development

### Шаг 4: Переразверни проект

1. Deployments → последний деплоймент
2. ••• → Redeploy
3. Подожди 2-3 минуты

## 🆘 Альтернативное решение

Если проблема не решается, можно **временно убрать проверку** из кода:

Удалить строки 17-40 в `src/app/api/auth/[...nextauth]/route.ts`

Но лучше сначала попробовать исправить переменные!

## 💡 Как проверить, что переменные правильные

После переразвертывания:
```bash
curl https://koloskovm08-rgb-aiplatform.vercel.app/api/auth/health
```

Должно быть:
```json
{
  "oauth": {
    "google": {
      "configured": true
    }
  }
}
```

И попробуй войти через Google — должно работать!

